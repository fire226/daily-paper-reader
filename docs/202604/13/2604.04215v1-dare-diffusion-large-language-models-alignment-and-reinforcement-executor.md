---
title: "DARE: Diffusion Large Language Models Alignment and Reinforcement Executor"
title_zh: DARE：扩散大语言模型对齐与强化执行器
authors: "Jingyi Yang, Yuxian Jiang, Xuhao Hu, Shuang Cheng, Biqing Qi, Jing Shao"
date: 2026-04-05
pdf: "https://arxiv.org/pdf/2604.04215v1"
tags: ["query:ai-ml"]
score: 9.0
evidence: 面向扩散大语言模型对齐与强化学习后训练的开源框架
tldr: 针对扩散大语言模型后训练生态碎片化问题，本文提出开源框架DARE。该框架整合了微调、偏好优化与强化学习流程，支持多种主流扩散模型，提供可复现评估与加速能力，为dLLMs对齐研究提供统一高效的开发底座。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.04215v1/fig-001.webp\", \"caption\": \"\", \"page\": 4, \"index\": 1, \"width\": 1394, \"height\": 815}, {\"url\": \"assets/figures/arxiv/2604.04215v1/fig-002.webp\", \"caption\": \"\", \"page\": 6, \"index\": 2, \"width\": 3994, \"height\": 1500}, {\"url\": \"assets/figures/arxiv/2604.04215v1/fig-003.webp\", \"caption\": \"\", \"page\": 9, \"index\": 3, \"width\": 10649, \"height\": 3305}]"
motivation: 现有扩散大语言模型的后训练代码与评估脚本高度碎片化，导致研究迭代缓慢、复现困难且算法难以公平对比。
method: 本文构建了基于verl和OpenCompass的统一开源框架DARE，将各类微调与强化学习算法整合至共享执行栈中。
result: 实验表明该框架在多款主流扩散模型上实现了广泛的算法覆盖、可复现的基准测试与显著的训练加速。
conclusion: DARE成功为扩散大语言模型的后训练方法研发、对比与部署提供了可复用的高效基础设施。
---

## 摘要
扩散大语言模型（dLLMs）正成为主流自回归模型的有力替代方案，其通过迭代去噪与并行生成机制，取代了严格的序列词元生成方式。然而，其开源生态在不同模型家族间仍显碎片化，尤其是在后训练流水线方面，强化学习目标、rollout 实现与评估脚本通常仅作为特定论文的独立代码库发布。这种碎片化拖慢了研究迭代速度，增加了复现的工程负担，并使得不同算法间的公平比较变得困难。本文提出 \textbf{DARE}（\textbf{d}LLMs \textbf{A}lignment and \textbf{R}einforcement \textbf{E}xecutor，扩散大语言模型对齐与强化执行器），这是一个面向 dLLMs 后训练与评估的开放框架。基于 verl~\cite{sheng2024hybridflow} 与 OpenCompass~\cite{2023opencompass} 构建，DARE 在统一的执行栈下整合了监督微调、参数高效微调、偏好优化以及面向 dLLM 的强化学习，同时支持掩码与块扩散语言模型。针对 LLaDA、Dream、SDAR 及 LLaDA2.x 等代表性模型家族，DARE 提供了广泛的算法覆盖、可复现的基准评估以及实用的加速优化。大量实证结果表明，DARE 可作为可复用的研究基座，用于开发、比较与部署面向当前及新兴 dLLMs 的后训练方法。

## Abstract
Diffusion large language models (dLLMs) are emerging as a compelling alternative to dominant autoregressive models, replacing strictly sequential token generation with iterative denoising and parallel generation dynamics. However, their open-source ecosystem remains fragmented across model families and, in particular, across post-training pipelines, where reinforcement learning objectives, rollout implementations and evaluation scripts are often released as paper-specific codebases. This fragmentation slows research iteration, raises the engineering burden of reproduction, and makes fair comparison across algorithms difficult. We present \textbf{DARE} (\textbf{d}LLMs \textbf{A}lignment and \textbf{R}einforcement \textbf{E}xecutor), an open framework for post-training and evaluating dLLMs. Built on top of verl~\cite{sheng2024hybridflow} and OpenCompass~\cite{2023opencompass}, DARE unifies supervised fine-tuning, parameter-efficient fine-tuning, preference optimization, and dLLM-specific reinforcement learning under a shared execution stack for both masked and block diffusion language models. Across representative model families including LLaDA, Dream, SDAR, and LLaDA2.x, DARE provides broad algorithmic coverage, reproducible benchmark evaluation, and practical acceleration. Extensive empirical results position that DARE serves as a reusable research substrate for developing, comparing, and deploying post-training methods for current and emerging dLLMs.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）
- **研究动机**：扩散大语言模型（dLLMs）正成为自回归模型的有力替代方案，但其开源生态（尤其是后训练流水线）高度碎片化。各论文的强化学习目标、rollout 实现与评估脚本通常以独立代码库形式发布。
- **核心痛点**：碎片化导致研究迭代缓慢、复现工程负担重、跨算法公平对比困难。此外，传统 LLM 的 RL 框架无法直接迁移，因为 dLLMs 依赖迭代去噪、双向/块状注意力机制及特定的似然代理目标。
- **整体含义**：本文提出 **DARE** 框架，旨在为 dLLMs 构建一个统一、开源、可复用的后训练与评估基础设施，将“论文+定制代码”的孤立范式转变为“共享执行器内的标准化算法对比”。

### 2. 论文提出的方法论
- **核心思想**：基于 `verl`（分布式训练）和 `OpenCompass`（评估）构建统一执行栈，通过抽象化工作流隔离算法特异性逻辑，同时兼容掩码扩散（MDLM）与块扩散（BDLM）两大主流范式。
- **关键技术细节**：
  - **统一训练执行器**：将流程抽象为 `worker`（功能角色）、`dataflow`（数据流向）和 `workflow`（优化循环）。共享类 PPO 的优化骨架，仅通过少量 Hook 暴露扩散特异性组件（如前向加噪过程、轨迹构建规则、似然估计器、策略损失）。
  - **算法集成**：内置 SFT、PEFT、偏好优化及多种 dLLM 专属 RL 算法（VRPO, D1, Coupled-GRPO, MDPO, CJ-GRPO, SPG, BGPO, EBPO），涵盖 ELBO 基、ELBO 启发式及轨迹感知目标。
  - **系统级加速优化**：
    - *MDLM 路径*：**解耦训练与 rollout 的注意力后端**。Rollout 采用 Fast-dLLM + FlashAttention（利用 KV Cache 加速采样）；训练采用 `flash_attn_varlen_func` 跳过 Padding 计算。端到端加速约 4 倍。
    - *BDLM 路径*：Rollout 集成 LMDeploy/SGLang，训练采用 FlexAttention 与融合损失算子，支持在线策略同步。加速约 14 倍。
- **公式/算法流程**：框架本身不提出新优化目标，而是统一实现现有目标。例如 MDLM 基于负证据下界（NELBO）进行去噪预测训练；BDLM 采用块级 NELBO。RL 算法通过蒙特卡洛采样或单步/多步去噪近似 ELBO 或其变体，计算优势函数并更新策略网络。

### 3. 实验设计
- **数据集/场景**：数学推理（GSM8K, MATH）、代码生成（HumanEval, MBPP, DeepCoder 过滤集）、规划任务（Countdown, Sudoku）。
- **Benchmark**：涵盖通用知识、常识、数学、代码等 11 项主流基准（MMLU, MMLU-Pro, HellaSwag, ARC-C, GPQA, AIME24/25, OlympiadBench 等）。
- **对比方法**：
  - *基座模型*：LLaDA-8B, Dream-7B, SDAR-8B/30B, LLaDA2.0/2.1-mini。
  - *RL 算法*：Baseline, d1, Coupled-GRPO, VRPO, CJ-GRPO, SPG, BGPO。
  - *控制变量*：统一超参数（rollout 组大小=8，块长=32，默认关闭 KL 正则，MC 采样数=16 等），确保对比公平。

### 4. 资源与算力
- 文中**未明确说明**具体的 GPU 型号、数量、集群规模或单次实验的训练时长。仅提供了算法层面的超参数配置（如扩散步数 256/128、最大响应长度 512/256、训练 1 个 epoch 等）。

### 5. 实验数量与充分性
- **实验规模**：包含 6 款主流 dLLM 的基线性能复现、2 款核心模型（LLaDA, Dream）在 3 类任务上的多算法对比实验，以及详细的训练曲线稳定性分析。
- **充分性与公平性**：实验设计充分且客观。通过统一执行栈和固定超参数，消除了底层实现差异带来的干扰，实现了真正的“控制变量”对比。结果明确展示了算法性能的“任务依赖性”和“模型依赖性”，避免了单一指标片面结论，具有较高的学术参考价值。

### 6. 论文的主要结论与发现
- **框架有效性**：DARE 成功整合了碎片化的 dLLM 后训练生态，提供可复现的基准测试与显著的系统加速，验证了统一执行器的实用价值。
- **算法无绝对赢家**：不同 RL 算法在不同任务和基座上表现各异（如 CJ-GRPO 擅长数学，VRPO/Coupled-GRPO 擅长代码/规划），不存在通用最优解。
- **稳定性洞察**：d1、Coupled-GRPO 和 CJ-GRPO 的奖励曲线更稳定；而基于 ELBO 的方法（如 SPG、BGPO）对蒙特卡洛采样预算敏感，估计方差大，易在训练后期出现奖励崩溃（Reward Collapse）。这提示未来需开发更稳定、高效的 ELBO 估计器。

### 7. 优点
- **首创性与工程价值**：首个专为 dLLMs 设计的统一后训练与评估开源框架，大幅降低研究门槛与复现成本。
- **架构设计精巧**：通过“共享骨架+特异性 Hook”的设计，兼顾了通用性与扩展性；解耦训练/Rollout 注意力后端的设计极具系统洞察力，针对性解决性能瓶颈。
- **实证分析深入**：不仅报告最终分数，还通过训练曲线揭示了算法稳定性与目标估计方差之间的内在联系，为社区提供了宝贵的算法选型与改进指导。

### 8. 不足与局限
- **算力信息缺失**：未公开硬件配置与训练成本，不利于工业界评估实际部署开销与复现门槛。
- **模型覆盖边界**：目前仅支持文本模态的 MDLM/BDLM，尚未扩展至视觉/多模态扩散模型或连续扩散范式。
- **算法稳定性未根本解决**：框架暴露了 ELBO 类方法的脆弱性，但未提供内置的方差缩减或稳定性增强机制，仍需依赖外部算法改进。
- **长期维护压力**：dLLM 架构迭代迅速，框架需持续跟进新模型家族与 RL 变体，社区贡献与版本维护将是长期挑战。

（完）
