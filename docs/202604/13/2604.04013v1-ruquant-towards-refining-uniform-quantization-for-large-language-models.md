---
title: "RUQuant: Towards Refining Uniform Quantization for Large Language Models"
title_zh: RUQuant：面向大语言模型的均匀量化优化方法
authors: "Han Liu, Haotian Gao, Changya Li, Feng Zhang, Xiaotong Zhang, Wei Wang, Hong Yu"
date: 2026-04-05
pdf: "https://arxiv.org/pdf/2604.04013v1"
tags: ["query:ai-ml"]
score: 9.0
evidence: 改进均匀量化方案的后训练量化方法，处理LLM非均匀激活分布
tldr: 针对大模型均匀量化因激活分布不均致精度下降的问题，本文提出RUQuant。该方法基于Lloyd-Max条件，通过两阶段正交变换映射激活并微调全局反射矩阵。实验表明其无需微调即可快速实现接近全精度的量化性能。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.04013v1/fig-001.webp\", \"caption\": \"\", \"page\": 2, \"index\": 1, \"width\": 3696, \"height\": 3697}, {\"url\": \"assets/figures/arxiv/2604.04013v1/fig-002.webp\", \"caption\": \"\", \"page\": 2, \"index\": 2, \"width\": 3696, \"height\": 3697}, {\"url\": \"assets/figures/arxiv/2604.04013v1/fig-003.webp\", \"caption\": \"\", \"page\": 11, \"index\": 3, \"width\": 3696, \"height\": 3697}, {\"url\": \"assets/figures/arxiv/2604.04013v1/fig-004.webp\", \"caption\": \"\", \"page\": 11, \"index\": 4, \"width\": 3696, \"height\": 3697}]"
motivation: 现有均匀量化方法因激活值分布非均匀导致量化点偏离区间中点，从而引发显著的精度损失。
method: 提出两阶段正交变换方法RUQuant，先利用复合正交矩阵将分块激活映射至均匀目标向量，再通过微调全局Householder反射进一步降低量化误差。
result: "在13B大模型上，该方法无需微调即可在约一分钟内实现W6A6和W4A4量化下分别达到全精度99.8%和97%的准确率。"
conclusion: RUQuant有效克服了均匀量化中的分布偏移问题，为大语言模型的高效无损压缩提供了可扩展的实用方案。
---

## 摘要
大语言模型（LLMs）规模与复杂性的不断增长给部署效率带来了严峻挑战，尤其是在资源受限的条件下。训练后量化（PTQ）作为一种无需重新训练即可压缩模型的实用方案应运而生。尽管现有方法主要针对权重和激活值采用均匀量化方案，但由于激活值分布的非均匀特性，这些方法往往会导致显著的精度下降。本文基于Lloyd-Max最优性条件，从理论视角重新审视了激活值量化问题。我们将核心问题归结为量化区间内激活值分布的非均匀性，这导致在Lloyd-Max准则下的最优量化点偏离区间中点。为解决该问题，我们提出了一种两阶段正交变换方法——RUQuant。在第一阶段，激活值被划分为若干块。每块通过由Householder反射和Givens旋转构建的复合正交矩阵，映射至均匀采样的目标向量。在第二阶段，利用Transformer输出差异对全局Householder反射进行微调，以进一步最小化量化误差。实验结果表明，我们的方法在无需模型微调的情况下即可实现接近最优的量化性能：对于13B参数的大语言模型，RUQuant在约一分钟内即可完成量化，并在W6A6配置下达到全精度准确率的99.8%，在W4A4配置下达到97%。经过微调的变体版本取得了更高的精度，进一步验证了该方法的有效性与可扩展性。

## Abstract
The increasing size and complexity of large language models (LLMs) have raised significant challenges in deployment efficiency, particularly under resource constraints. Post-training quantization (PTQ) has emerged as a practical solution by compressing models without requiring retraining. While existing methods focus on uniform quantization schemes for both weights and activations, they often suffer from substantial accuracy degradation due to the non-uniform nature of activation distributions. In this work, we revisit the activation quantization problem from a theoretical perspective grounded in the Lloyd-Max optimality conditions. We identify the core issue as the non-uniform distribution of activations within the quantization interval, which causes the optimal quantization point under the Lloyd-Max criterion to shift away from the midpoint of the interval. To address this issue, we propose a two-stage orthogonal transformation method, RUQuant. In the first stage, activations are divided into blocks. Each block is mapped to uniformly sampled target vectors using composite orthogonal matrices, which are constructed from Householder reflections and Givens rotations. In the second stage, a global Householder reflection is fine-tuned to further minimize quantization error using Transformer output discrepancies. Empirical results show that our method achieves near-optimal quantization performance without requiring model fine-tuning: RUQuant achieves 99.8% of full-precision accuracy with W6A6 and 97% with W4A4 quantization for a 13B LLM, within approximately one minute. A fine-tuned variant yields even higher accuracy, demonstrating the effectiveness and scalability of our approach.

---

## 论文详细总结（自动生成）

### 1. 核心问题与整体含义（研究动机和背景）
- **研究背景**：大语言模型（LLMs）参数量激增导致部署时的显存与计算开销巨大。训练后量化（PTQ）因无需重新训练而成为主流压缩方案，其中权重-激活联合量化（如 W4A4/W6A6）对推理加速至关重要。
- **核心问题**：现有方法普遍采用均匀量化器，但 LLM 的激活值分布呈现高度非均匀性（存在显著离群值与通道尺度差异）。基于 Lloyd-Max 最优性条件分析，非均匀分布会导致区间最优量化点（质心）偏离均匀量化器的区间中点，从而破坏均匀量化的理论假设，引发显著精度损失。
- **整体含义**：本文从理论层面重新审视激活量化瓶颈，提出通过正交变换将非均匀激活分布“重塑”为近似均匀分布，使均匀量化器能够逼近 Lloyd-Max 理论最优解，从而在极低开销下实现高精度、低比特量化。

### 2. 论文提出的方法论
- **核心思想**：利用正交变换的保范特性，在不改变线性层数学等价性（`Y = WQᵀ · QX`）的前提下，将激活值与权重映射至各向同性、均值均匀的分布空间，消除均匀量化器的分布失配问题。
- **关键技术细节**：
  - **分块复合正交映射**：将激活矩阵按固定块大小（`B=128`）划分，通过 Householder 反射与 Givens 旋转构建复合正交矩阵，将采样激活向量精确映射至均匀分布的目标向量。
  - **Zigzag 置换**：在块间引入随机置换矩阵打乱元素顺序，降低块间方差，提升整体分布均匀性。
  - **可学习全局反射微调**：引入仅含 `d` 个参数的 Learnable Householder 向量 `θ`，通过最小化量化前后 Transformer 块输出的 Frobenius 范数差异（公式 13）进行轻量级梯度优化，进一步对齐量化损失与模型损失。
- **算法流程**：
  1. **阶段一（分布均匀化）**：对每个 Transformer 块，采样激活向量 → 迭代计算 Householder 反射与 Givens 旋转矩阵 → 组合为块共享正交矩阵 `Qc` → 作用于权重与激活 → 执行 Zigzag 置换与二次旋转。
  2. **阶段二（误差微调，可选）**：初始化全局反射向量 `θ` → 基于校准数据计算量化前后块输出差异 → 使用 Adam 优化 `θ`（默认 20 epoch, lr=1e-2）→ 完成最终量化。

### 3. 实验设计
- **数据集与场景**：
  - **困惑度（PPL）**：WikiText2、C4 数据集。
  - **零样本问答（Zero-shot QA）**：PIQA、ARC-E、ARC-C、BoolQ、HellaSwag、WinoGrande。
  - **综合基准**：MMLU（0-shot 与 5-shot）。
- **Benchmark 与评估指标**：以 FP16 全精度为参考基准，主要指标为困惑度（↓）、零样本准确率（↑）、推理延迟加速比、峰值显存占用。
- **对比方法**：SmoothQuant、OmniQuant、QLLM、DuQuant 等主流权重-激活联合量化方法。

### 4. 资源与算力
- **硬件配置**：主实验使用 NVIDIA L20 GPU（40GB 显存）；推理加速与显存测试使用单张 NVIDIA RTX 3090。
- **时间开销**：无需微调的 RUQuant 对 13B 模型量化仅需约 **1 分钟**；微调变体需额外 20 个 epoch（batch size=1）。
- **算力说明**：文中未明确给出总 GPU 时数或并行训练卡数，但强调算法为单卡友好型，且校准数据仅使用 128 条 2048-token 片段，整体算力需求极低。

### 5. 实验数量与充分性
- **实验规模**：涵盖 3 个模型家族（LLaMA1/2/3）、5 种参数量级（7B~30B）、2 种量化精度（W4A4/W6A6）、2 个 PPL 数据集、6 个 QA 任务、MMLU 双设置，以及消融实验、运行时间、推理加速、显存占用等专项测试，核心实验配置超 20 组。
- **充分性与客观性**：
  - **充分**：覆盖不同模型规模、架构代际、任务类型与量化比特，验证了方法的泛化性与可扩展性。
  - **客观公平**：统一使用 WikiText2 的 128 条片段作为校准集，严格遵循零样本评估协议，对比基线采用相同量化设置（RTN+Clip），指标选取全面且符合领域标准。

### 6. 主要结论与发现
- RUQuant 在无需微调的情况下，13B 模型 W6A6 量化可保留全精度 **99.8%** 的准确率，W4A4 保留 **97%**，且全流程仅需约 1 分钟。
- 在 PPL 与 Zero-shot QA 任务上，RUQuant 全面优于 SmoothQuant、OmniQuant、QLLM 与 DuQuant，部分场景逼近 FP16 性能。
- 显存占用降低约 3.3~3.6 倍，Prefill 阶段推理加速 1.8~2.1 倍，与 DuQuant 相当。
- 微调变体可进一步提升精度，但部分零样本任务出现轻微波动，表明校准数据分布对微调稳定性有一定影响。

### 7. 优点
- **理论扎实**：首次从 Lloyd-Max 最优性条件出发，严格推导均匀量化失效的数学根源，并给出正交变换逼近理论最优解的证明（Theorem 1 & 2）。
- **高效轻量**：闭式正交矩阵构造 + 分块共享 + Zigzag 置换，参数量仅 `B²`，计算复杂度降至 `O(BdN)`；全局微调仅优化 `d` 维向量，开销极小。
- **灵活可配置**：提供“免微调极速版”与“微调高精度版”双模式，适配不同部署场景的精度-延迟权衡需求。
- **实验严谨**：多维度评估（精度、速度、显存、消融）且开源代码，复现门槛低。

### 8. 不足与局限
- **微调稳定性风险**：W4A4 微调版在部分零样本任务上准确率略低于免微调版（如 LLaMA2-7B 平均下降 1.16%），提示可能存在校准集过拟合或优化敏感性问题。
- **启发式组件**：分块大小（`B=128`）、Zigzag 置换次数、采样迭代次数等超参数依赖经验设定，缺乏严格的理论误差界分析。
- **架构覆盖有限**：实验仅基于 LLaMA 系列 Dense Transformer 架构，未验证 MoE、非自回归或视觉-语言多模态模型的适用性。
- **硬件依赖**：推理加速数据基于定制 W4A4 内核测得，实际部署收益高度依赖底层算子实现与硬件支持，通用性受限。

（完）
