---
title: "SLaB: Sparse-Lowrank-Binary Decomposition for Efficient Large Language Models"
title_zh: SLaB：面向高效大语言模型的稀疏-低秩-二值分解
authors: "Ziwei Li, Yuang Ma, Yi Kang"
date: 2026-04-06
pdf: "https://arxiv.org/pdf/2604.04493v1"
tags: ["query:ai-ml"]
score: 9.0
evidence: 用于大模型压缩与部署的稀疏低秩二值分解
tldr: "针对大语言模型部署时计算与内存开销巨大的问题，本文提出SLaB框架，将线性层权重分解为稀疏、低秩和二进制三个互补矩阵。该方法无需重新训练，利用激活感知剪枝分数指导分解，在50%压缩率下显著降低困惑度并提升零样本任务准确率，实现了高效且高性能的模型压缩。"
source: arxiv
selection_source: fresh_fetch
motivation: 现有模型压缩方法在高压缩率下往往难以保持模型性能，导致大语言模型部署面临巨大计算与内存挑战。
method: 提出SLaB框架，利用激活感知剪枝分数将线性层权重分解为稀疏、低秩和二进制矩阵，且无需重新训练。
result: "在Llama系列模型上的实验表明，SLaB在50%压缩率下困惑度降低最高达36%，零样本任务准确率较基线提升最高8.98%。"
conclusion: SLaB通过创新的三元矩阵分解策略，在无需重训的前提下实现了高压缩率与高性能的平衡，为大模型高效部署提供了有效方案。
---

## 摘要
大语言模型（LLMs）的快速发展因其巨大的计算与内存需求，带来了显著的部署挑战。尽管模型压缩（如网络剪枝）提供了潜在的解决方案，但大多数现有方法在高压缩率下往往难以维持良好的性能。为解决这一问题，我们提出了SLaB框架，该框架将每个线性层的权重分解为三个互补的组成部分：稀疏矩阵、低秩矩阵与二值矩阵。SLaB无需重新训练，并利用激活感知剪枝分数来指导分解过程。在Llama系列模型上的实验表明，SLaB取得了最先进的性能：在50%压缩率下，其困惑度较现有方法最多降低36%；在零样本任务上，其准确率较基线最多提升8.98%。

## Abstract
The rapid growth of large language models (LLMs) presents significant deployment challenges due to their massive computational and memory demands. While model compression, such as network pruning, offers potential solutions, most existing methods often fail to maintain good performance at high compression ratios. To address this, we propose SLaB, a novel framework that decomposes each linear layer weight into three complementary components: a sparse matrix, a low-rank matrix, and a binary matrix. SLaB eliminates the need for retraining and leverages activation-aware pruning scores to guide the decomposition process. Experiments on Llama-family models demonstrate that SLaB achieves state-of-the-art performance, reducing perplexity by up to 36% compared to existing methods at 50% compression and improving accuracy by up to 8.98% over the baseline on zero-shot tasks.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）
- **研究背景**：大语言模型（LLMs）参数量呈指数级增长，导致推理时的计算负载与内存占用极高，严重阻碍了其在边缘设备或资源受限环境中的部署。
- **现有痛点**：传统模型压缩方法（如网络剪枝、低秩分解）在高压缩率下往往引发严重的精度崩塌；单纯将稀疏化与低秩近似拼接效果不佳（如图1所示）；而依赖微调的压缩方法计算成本高昂。
- **核心问题**：如何在**无需重新训练**的前提下，突破高压缩率下的性能瓶颈，实现计算效率与模型精度的最优平衡。

### 2. 论文提出的方法论
- **核心思想**：提出 **SLaB（Sparse-Lowrank-Binary Decomposition）** 框架，将线性层权重矩阵 $W$ 分解为三个互补组件：稀疏矩阵 $W_S$（保留关键权重）、低秩矩阵 $W_L$（补偿全局结构信息）与二值矩阵 $W_B$（简化计算）。分解公式为：$W = W_S + W_L \odot W_B$（$\odot$ 为逐元素乘积）。
- **关键技术细节**：
  - **激活感知引导**：借鉴 Wanda 思想，利用前向传播的激活矩阵 $X$ 计算权重重要性分数，指导稀疏化过程。
  - **交替优化策略**：固定其中两个矩阵，迭代更新第三个矩阵，通常数次迭代即可收敛。
  - **$W_S$ 优化**：计算残差 $Y_S = W - W_L \odot W_B$，结合激活范数构造评分矩阵 $S_{ij} = |Y_{S,ij}| \cdot \|X_j\|_2$，按阈值硬剪枝。
  - **$W_B$ 优化**：通过数学引理证明，在权重近似零均值对称分布的假设下，二值矩阵的最优解可严格约束为 $\{+1, -1\}$，大幅降低存储与计算开销。
  - **$W_L$ 优化**：基于 Eckart-Young 定理对残差进行截断 SVD。实验验证 **Rank-1** 设置即可显著降低重构误差，兼顾效率。
  - **高效初始化**：采用 $W_B = \text{sign}(Y_{BL})$ 与 $|Y_{BL}|$ 的 Rank-1 SVD 结果联合初始化，避免陷入局部次优并减少迭代开销。
- **算法流程**：逐层前向获取激活值 $\rightarrow$ 初始化三组件 $\rightarrow$ 循环 $s$ 次交替更新 $W_S, W_L, W_B$ $\rightarrow$ 输出分解权重替换原层权重，全程无梯度反向传播。

### 3. 实验设计
- **模型与场景**：Llama-2 7B、Llama-3 8B、Llama-3.2 1B。
- **评估数据集与 Benchmark**：
  - **语言建模**：WikiText-2（评估困惑度 Perplexity，越低越好）。
  - **零样本推理**：ARC-C, ARC-E, BoolQ, HellaSwag, PIQA, RTE, WinoGrande（通过 LM-Eval-Harness 评估平均准确率，越高越好）。
  - **校准数据**：C4 数据集首分片中随机采样的 128 条长度为 2048 的文本序列。
- **对比基线**：当前 SOTA 免训练剪枝方法 **SparseGPT** 与 **Wanda**。
- **压缩配置**：非结构化稀疏（压缩率 50%、60%、70%、80%）与半结构化稀疏（2:4 与 4:8 模式，压缩率 50%）。

### 4. 资源与算力
- 论文正文及附录中**未明确说明**具体的 GPU 型号、数量、显存占用或单次压缩耗时。
- 由于 SLaB 属于免训练（Training-free）的逐层解析方法，其算力消耗主要集中在少量 SVD 分解与矩阵交替更新上，理论上远低于微调类压缩方案，但缺乏具体的硬件基准测试数据支撑。

### 5. 实验数量与充分性
- **实验规模**：涵盖 3 个模型规模 × 4 种非结构化压缩率 × 2 种半结构化模式 × 超参数探索（对比组大小、迭代次数）× 消融实验，总计约 30+ 组核心对比实验。
- **充分性与客观性**：
  - 实验覆盖了不同参数量级与压缩强度，验证了方法的鲁棒性。
  - 所有方法使用完全相同的校准数据集与评估协议，确保了公平对比。
  - 消融实验清晰剥离了 $W_S$、$W_L$、$W_B$ 的独立贡献，逻辑闭环完整。
  - 整体设计符合当前免训练压缩领域的标准范式，数据详实、结论客观。

### 6. 论文的主要结论与发现
- SLaB 在零微调条件下全面超越 SparseGPT 与 Wanda，达到当前免训练剪枝的 SOTA 水平。
- 在 50% 压缩率下，相比最优基线，WikiText-2 困惑度最高降低 **36%**，零样本任务平均准确率最高提升 **8.98%**。
- 低秩组件采用 Rank-1 即可在重构精度与计算开销间取得最佳平衡；二值矩阵的引入有效降低了低秩矩阵所需的秩，提升了硬件友好性。
- 稀疏、低秩与二值三者具有强互补性，联合优化能高效补偿高稀疏度下的信息损失。

### 7. 优点
- **理论严谨**：通过引理与命题严格推导了二值矩阵取 $\{+1, -1\}$ 的次优性及 Rank-1 初始化的合理性，非纯启发式设计。
- **部署友好**：完全免训练，无需反向传播与额外微调数据；二值矩阵仅需 1-bit 存储，低秩矩阵为向量外积，稀疏矩阵支持零值跳过，高度契合现代 AI 加速器架构。
- **泛化稳定**：在 1B 至 8B 不同规模模型及多种稀疏模式下均保持显著性能优势，超参数敏感度低（迭代 20 次即收敛）。

### 8. 不足与局限
- **模型覆盖有限**：仅在 Llama 家族验证，未扩展至 MoE 架构、多模态大模型或其他主流开源架构（如 Qwen、Mistral），跨架构泛化性待考。
- **免训练的固有上限**：作为 One-shot 方法，性能受限于校准数据分布与原始权重先验，无法通过微调进一步突破；在极高压缩率（如 80%）下性能仍出现明显衰减。
- **硬件实测缺失**：虽强调“硬件友好”，但未提供针对该分解格式的底层算子实现、推理延迟（Latency）实测或内存带宽分析，工程落地价值需进一步验证。
- **校准数据依赖风险**：性能高度依赖少量校准序列的激活统计，若校准数据与下游目标任务分布差异较大，可能引入剪枝偏差。

（完）
