---
title: "REAM: Merging Improves Pruning of Experts in LLMs"
title_zh: REAM：合并优于大语言模型中的专家剪枝
authors: "Saurav Jha, Maryam Hashemzadeh, Ali Saheb Pasand, Ali Parviz, Min-Joong Lee, Boris Knyazev"
date: 2026-04-06
pdf: "https://arxiv.org/pdf/2604.04356v1"
tags: ["query:ai-ml"]
score: 9.0
evidence: 专家合并方法以降低MoE大模型内存占用并提升部署效率
tldr: 针对MoE大模型部署的显存瓶颈，本文提出路由加权专家激活合并（REAM）方法。该方法通过分组合并专家权重替代直接剪枝，有效保留模型能力。实验表明，调整校准数据配比可优化选择与生成任务的权衡，REAM性能优于基线且逼近原始模型。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.04356v1/fig-001.webp\", \"caption\": \"\", \"page\": 7, \"index\": 1, \"width\": 1798, \"height\": 772}, {\"url\": \"assets/figures/arxiv/2604.04356v1/fig-002.webp\", \"caption\": \"\", \"page\": 9, \"index\": 2, \"width\": 2796, \"height\": 730}]"
motivation: 传统专家剪枝会严重损害模型性能，亟需一种能在降低显存占用的同时更好保留原始能力的压缩策略。
method: 提出REAM方法，利用路由权重对专家进行分组并合并其参数，而非直接移除。
result: 实验揭示了多项选择与生成任务的性能权衡受校准数据配比影响，且REAM在多数基准上显著优于现有方法。
conclusion: REAM通过权重合并有效缓解了MoE模型的部署压力，为模型压缩提供了兼顾性能与效率的更优方案。
---

## 摘要
混合专家（MoE）大语言模型（LLMs）是性能最优的架构之一。规模最大的模型通常包含数千亿参数，在部署时面临巨大的内存挑战。降低内存需求的传统方法包括权重剪枝和量化。受路由器加权专家激活剪枝（REAP）方法的启发，我们提出了一种新方法：路由器加权专家激活合并（REAM）。与直接移除专家不同，REAM 对专家进行分组并合并其权重，从而更好地保留原始性能。我们在多种 MoE 大语言模型上，针对多样化的多项选择（MC）问答和生成式（GEN）基准测试，将 REAM 与 REAP 及其他基线方法进行了对比评估。结果表明，MC 与 GEN 性能之间存在权衡关系，且该权衡取决于校准数据的混合比例。通过控制通用、数学和编程数据的混合比例，我们考察了该权衡的帕累托前沿，结果表明 REAM 通常优于基线方法，且在许多情况下可与原始未压缩模型相媲美。

## Abstract
Mixture-of-Experts (MoE) large language models (LLMs) are among the top-performing architectures. The largest models, often with hundreds of billions of parameters, pose significant memory challenges for deployment. Traditional approaches to reduce memory requirements include weight pruning and quantization. Motivated by the Router-weighted Expert Activation Pruning (REAP) that prunes experts, we propose a novel method, Router-weighted Expert Activation Merging (REAM). Instead of removing experts, REAM groups them and merges their weights, better preserving original performance. We evaluate REAM against REAP and other baselines across multiple MoE LLMs on diverse multiple-choice (MC) question answering and generative (GEN) benchmarks. Our results reveal a trade-off between MC and GEN performance that depends on the mix of calibration data. By controlling the mix of general, math and coding data, we examine the Pareto frontier of this trade-off and show that REAM often outperforms the baselines and in many cases is comparable to the original uncompressed models.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义
- **研究背景**：混合专家（MoE）架构通过稀疏路由大幅扩展了大语言模型的参数规模，但所有专家权重仍需完整加载至显存，导致部署时面临严峻的内存瓶颈。
- **核心问题**：现有压缩方法存在明显权衡。**剪枝**（如REAP）直接丢弃专家，易损失特定领域的关键知识；**合并**虽保留信息，但传统聚类易将功能不匹配的专家强行分组，且门控权重相加会引入不可约误差。
- **整体含义**：本文旨在打破“剪枝 vs 合并”的二元对立，提出一种静态、免训练的专家压缩框架，通过更精细的分组与权重融合策略，在显著降低显存占用的同时，最大程度保留模型的原始推理与生成能力。

### 2. 论文提出的方法论
- **核心思想**：以“伪剪枝分组 + 权重合并”替代直接删除专家。保留高重要性专家作为核心，将冗余专家吸收合并，并动态更新层间激活统计量。
- **关键技术细节**：
  - **聚合专家相似度**：结合门控Logits相似度与Softmax缩放的专家输出相似度。输出相似度使用门控概率对专家激活进行加权，使相似度度量更贴合MoE的实际路由机制。
  - **伪剪枝（Pseudo-pruning）分组**：按重要性排序选取前 $N'$ 个专家作为中心，贪婪地将最相似的至多 $C$ 个非中心专家分配给对应中心。该策略自然形成“少数大组 + 大量单例”的结构，避免传统合并中过度聚类导致的语义冲突。
  - **增强型权重对齐**：构建联合代价矩阵（激活距离 + 权重距离），通过匈牙利算法进行神经元排列对齐。兼顾数据驱动信号与参数结构，降低仅依赖激活或仅依赖权重带来的匹配噪声。
  - **顺序合并（Sequential Merging）**：每完成一层的专家合并后，立即用更新后的模型重新前向传播，为下一层提供真实的激活统计量，消除传统一次性收集统计量导致的“过期”偏差。
  - **门控权重处理**：直接移除非中心专家对应的门控权重行，避免传统合并中Logits累加带来的分布偏移。

### 3. 实验设计
- **测试模型**：Qwen3-30B-A3B-Instruct（128专家）、Qwen3-Next-80B系列（512专家）、GLM-4.5-Air（128专家）。
- **压缩设置**：每层专家数减少 25%（如128→96）与 50%（128→64）。
- **校准数据**：混合 C4（通用文本）、NuminaMath（数学）、The-Stack-Smol（代码），共设计 10 种不同配比，用于计算专家重要性与相似度。
- **Benchmark**：
  - **多项选择（MC）**：8项（ARC、BoolQ、HellaSwag、MMLU等），侧重判别与常识推理。
  - **生成式（GEN）**：6项（IFEval、AIME25、GSM8K、GPQA、HumanEval、LiveCodeBench），侧重指令遵循、数学与代码生成。
- **对比基线**：Freq（路由频率剪枝）、REAP（路由器加权激活剪枝）、HC-SMoE（层次聚类合并）。

### 4. 资源与算力
- **训练/微调算力**：本文属于**静态压缩（Static Compression）**，压缩后不进行任何微调或训练，因此无训练阶段的GPU算力消耗。
- **合并过程开销**：以 Qwen3-30B 为例，合并过程显存占用约 30 GB VRAM。非顺序合并耗时约 1 小时，本文的顺序合并耗时约 1.5 小时。计算资源致谢部分提及使用了 Calcul Québec 与 Compute Canada 集群，但未公开具体GPU型号与节点数量。

### 5. 实验数量与充分性
- **实验规模**：涵盖 4 个主流MoE模型 × 2 种压缩率 × 10 种校准配比 × 4 种方法，形成数百组对比配置；包含完整的组件消融实验、帕累托前沿分析、相关性分析。
- **充分性与客观性**：实验设计高度系统化。所有方法在完全相同的校准数据与评估流程下运行，且统一采用“压缩后零微调”设定，排除了额外训练带来的性能干扰。消融实验逐一验证了各模块贡献，帕累托分析量化了方法在不同任务偏好下的鲁棒性，整体评估严谨、公平且具备强可复现性。

### 6. 论文的主要结论与发现
- **性能优势**：REAM 在生成式（GEN）基准上全面优于 REAP、HC-SMoE 与 Freq。在 25% 压缩率下，REAM 的 GEN 平均分仅比原始模型低约 1.1 分，实现近乎无损压缩。
- **MC-GEN 权衡规律**：校准数据配比直接决定压缩后模型的能力偏向。C4 数据比例与 MC 成绩强正相关、与 GEN 强负相关；代码数据则呈现相反趋势；数学数据对两者影响均较弱。
- **帕累托前沿领先**：REAM 的 MC×GEN 超体积（HV）指标最高，且 Pareto 最优配置占比最大，证明其在任意 MC 性能底线要求下，均能提供更优的 GEN 表现。
- **组件有效性**：顺序合并与联合激活/权重对齐带来稳定增益；伪剪枝策略有效防止了功能错配；移除门控权重行避免了传统合并的固有误差。

### 7. 优点
- **方法创新**：提出“伪剪枝”与“顺序合并”机制，巧妙融合剪枝的稀疏性与合并的信息保留优势，解决了传统合并的聚类僵化与统计量过期问题。
- **部署友好**：纯静态、免训练压缩，无需额外算力、私有数据或微调流程，可直接集成至现有模型部署管线。
- **分析深入**：首次系统量化了校准数据分布对 MoE 压缩后 MC/GEN 能力的系统性影响，为工业界按需压缩提供了明确的数据配比指导。
- **评估全面**：同时覆盖判别式与生成式任务，结合帕累托分析与相关性检验，结论具备强说服力。

### 8. 不足与局限
- **校准数据敏感性**：模型性能高度依赖校准集配比，实际部署前需针对目标领域（如代码、数学或通用对话）精心调配数据，增加了工程调参成本。
- **特定任务性能衰减**：高难度推理任务（如 GPQA）在压缩后仍有显著分数下降，表明部分深层专家知识难以通过线性合并完全保留。
- **基准方差风险**：部分生成式基准（如 AIME25）样本量较小，单次评估结果可能存在统计波动，需更大规模测试集验证稳定性。
- **计算开销与扩展性**：顺序合并虽提升精度，但带来约 50% 的额外前向计算时间；本文仅聚焦专家合并阶段，未探索与量化、低秩分解等后续压缩技术的联合优化潜力。

（完）
